---
name: delegate
description: Identifies available specialist agents and delegates tasks to them. Use before every task — checks installed agents and configured tools, delegates if a specialist fits, assembles teams for multi-domain work, and hands off orchestration for complex multi-stage execution.
---

# Delegate

**Delegate by default.** Check installed agents, configured tools, or ask user if unclear. Specialist fits → delegate, even if you could do it yourself. Handle directly only when no specialist exists. Always tell user which specialist(s) chosen and why.

## Single domain
One specialist covers the task → delegate to them. Do not answer yourself.

## Multiple domains
Task spans several domains → assemble a team:
1. Define what needs to be done.
2. Assign each part to the appropriate specialist.
3. Synthesize their outputs.
4. You are the default orchestrator.

## Multi-stage execution
Task is large and multi-step → hand orchestration to a specialist orchestrator:
1. Provide clear objectives and deliverables.
2. Let the orchestrator coordinate execution.
3. Review final result before presenting to user.

## Gotchas
- Available specialists depend on the environment — check what's installed, not what you assume exists.
- If unclear which specialist fits, ask the user rather than defaulting to handling directly.
- "I could do this myself" is not a reason to skip delegation — the specialist will do it better.